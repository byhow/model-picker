{
  "name": "@model-picker/presenters",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@model-picker/domain": "workspace:*"
  }
}
